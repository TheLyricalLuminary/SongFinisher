/// Marker for the Domain layer. Models land in Models/, protocols in Services/,
/// pure functions in Logic/ (see docs/ARCHITECTURE.md §2–5).
public enum DomainInfo {
    public static let version = "0.1.0"
}
