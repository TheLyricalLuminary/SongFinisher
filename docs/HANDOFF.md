# Song Finisher — handoff

Everything an agent picking this up cold needs: what the app is, how it actually works,
where the traps are, and what is not done. Written 1 Aug, revised 3 Aug 2026.

`docs/ARCHITECTURE.md` is the design reference — read it second, and note that its §9 was
rewritten once already because it specified a cloud tier that was never built. This
document is the operational one: the things that bite.

---

## 1. What the app is

You play or sing a musical phrase. The app listens, works out where the phrase ended and
what its rhythm and stresses are, and suggests lyric lines built to fit **that specific
phrase** — right syllable count, stresses landing on the strong beats, mood inferred from
how you played it.

Author: Mark Amigoni, singer-songwriter, wrote the book *The Song Finisher* (2026). The
book is not decoration — its "Trigger Word Library" and its "lock and move" method are
implemented (`TriggerWords.swift`, flow mode).

Everything runs on device. **There is no networking code in the app at all.** The privacy
policy says so, and that claim must stay true — do not add a network call without
revisiting `site/privacy.html`.

---

## 2. Repository shape

| Package | What it does | Depends on |
|---|---|---|
| `Domain` | Pure models + scoring. No I/O, no Apple frameworks beyond Foundation. | — |
| `MelodyKit` | DSP. Pitch (YIN), onsets (spectral flux), tempo, chords, polyphony, phrase segmentation. AVFoundation + Accelerate. | Domain |
| `LyricEngine` | Lyric generation: the offline assembler, the Foundation Models provider, the lexicon, word sparks. | Domain |
| `PersistenceKit` | SwiftData store. | Domain |

App targets live at the repo root (`SongFinisher/`, `Features/`), generated by
**XcodeGen** from `project.yml`. Targets: `SongFinisher` (iOS), `SongFinisherMac`,
`SongFinisherWidgets` (Live Activity), `SongFinisherTests`.

`AppServices` (`SongFinisher/AppServices.swift`) is the composition root — the only place
concrete service types are named. ViewModels receive protocol existentials. `.fakes()`
mirrors `.live()` for tests and previews.

---

## 3. The pipeline, end to end

```
mic → AudioCaptureService → AnalysisChain (pitch/onset/amplitude per frame)
    → PhraseSegmenter or RhythmSegmenter → Phrase
    → ProsodyDeriving → PhraseSpec  (syllable budget + stress map + emotion + durations)
    → LyricProviding → [LyricCandidate]
    → CandidateRanking → [RankedCandidate] → the card
```

Two input modes, chosen automatically by `PolyphonyDetector`:

- **Melodic** (voice, single notes): syllable budget comes from the note count.
- **Rhythmic** (strummed chords): there is no melody to count, so the budget comes from
  the onset grid × the singer's chosen **density** (Sparse 1.0 / Medium 1.5 / Dense 2.0
  syllables per onset). This is why the "syllables per strum" picker appears.

### Two generation tiers

`AppServices.bestAvailableLyricProvider` picks at composition time:

- **Apple Intelligence hardware** → `FoundationModelsLyricProvider` (on-device LLM, iOS/macOS 26+).
  Cards badge **ON-DEVICE AI**. First call pays a real model-load cost — several seconds —
  which is why `LyricProviding.prewarm()` (default no-op, overridden here) fires when a
  listening session starts. A spinner right after launch is a cold start, not a hang.
- **Everywhere else, and free users past their daily allowance** → `OfflineLyricProvider`,
  the deterministic assembler. Cards badge **OFFLINE DRAFT**.

The premium provider **falls back internally** on ineligible hardware, so
`services.lyrics.kind` is what was *composed*, and `candidate.provider` is what *actually
ran*. Only the latter is honest after the fact. `SessionViewModel.lastProviderKind` seeds
from the former and corrects from the latter.

**There is no third tier and no remote tier.** An Anthropic-backed cloud provider was
designed and never built; `ProviderKind.claude`, `APIKeyProviding`, `HTTPPerforming` and
the network error cases have all been deleted, so "audio never leaves the device" is a
property of what exists rather than a promise. A bundled quantized open-weights model for
non-Apple-Intelligence hardware is the anticipated third tier — one more `LyricProviding`
conformance, still on-device. Its binding constraint is peak resident memory on a 4 GB
A15, which nobody has measured.

---

## 4. The offline assembler — read this before touching it

`PhraseAssembler.assemble` fills POS templates with beam search so concatenated stress
aligns to the phrase's stress map. Deterministic given a seed.

The lexicon (`Resources/lexicon.bin`, SFLX v1, 35,346 words) is built by
`tools/build_lexicon.py` from CMUdict + Moby POS + wordfreq Zipf + VADER valence.

**Moby's POS tags are dirty, and most of the guard code exists because of it.** It tags
"a" as a noun (the letter, the grade, the musical note), "your" as an adjective, "l" as a
noun. These are the highest-frequency words in English, so they sit at the top of every
Zipf-sorted bucket and the beam grabs them first. Every one of these guards is load-bearing
and has a regression test:

- `contentSlotStopWords` — function words barred from open-class slots
- article-tagged words barred from non-article slots (`"half a your cans"`)
- multi-syllable `-ing`/`-ed` barred from bare-verb slots (`"I upgrading a bore"`)
- single letters and profanity barred from content slots
- `isAcceptable`: no repeated words, no ending on a function word, no `"a"` before a
  vowel, no singular determiner before a plural

**Performance trap:** these membership tests run in the beam search's innermost loop,
hundreds of thousands of times per call. They are `Set<Int>` of lexicon row numbers,
resolved once at init or once per assemble — *not* `Set<String>`. Do not "simplify" them
back to string lookups; an unoptimized build cannot inline the hashing away.

**Template bounds trap:** a template only applies when
`minSyllables <= target <= maxSyllables`. Every frame used to need three slots, so a
1- or 2-syllable phrase (one strum on Sparse, or a two-note hum) matched **nothing**, the
pool came back empty, no error was thrown because nothing failed, and the UI waited
forever. Fixed with four short frames, and there is now coverage at 1 and 2 — all the
older assembler tests start at 3, which is exactly why it shipped.

### Word sparks

The offline tier's honest offering. A word-level assembler cannot guarantee *meaning* —
it produces "warm in a doom" — so on that tier the session screen **leads with sparks**
(evocative words that fit the phrase's emotion and beat) and demotes the generated line
under "OR START FROM A DRAFT". On the AI tier the line genuinely is the answer and leads.

`LexiconSparkProvider` fills **two tiers in order**: curated words from the book's Trigger
Word Library, *shuffled* with the phrase seed, then statistical picks sorted by valence
fit and freshness. It was briefly a single score with a `+5 if curated` bonus — that made
every trigger word tie, froze the panel order, and returned the same six words phrase
after phrase. Shuffling keeps curation absolute *and* turns the vocabulary over.

Statistics alone rank "blockbuster", "heroine" and "brilliantly" as great Joy words. They
score well and no songwriter would ever use them. That is what the curated list is for.

**Known tradeoff, undecided:** the book maps eight app emotions onto five states, so
`joy`/`release` share a curated pool, as do `melancholy`/`longing`/`nostalgia` — those
pairs produce identical spark panels. Faithful to the book; may or may not be what a
songwriter expects. Mark's call.

---

## 5. Flow mode and the song sheet

The hands-free contract. Mark's own framing: *"when I'm playing guitar and singing I don't
want to have to stop in the middle of something creative to click the mouse."* Everything
here follows from that.

With flow mode on, **every line is written onto the song sheet the moment it arrives.**
The writer never taps anything. Kept lines stack above the suggestion in lyric-page type,
newest emphasised, and the view scrolls to follow.

Two rules make that safe:

1. **One line per phrase.** `SessionMemory.record` upserts by `phraseID`, so Regenerate /
   More Like This / Different Emotion / ±1 syllable all *rewrite* that phrase's line.
   Without this, fiddling with one line would leave six copies of it.
2. **Replacement preserves position**, in memory *and* on disk. `SongStoring.replaceLine`
   rewrites a row in place; remove-then-append would send the rewritten line to the end of
   the song, and `SongSheetView` and the text export both read those rows — the saved song
   would disagree with the order the writer watched being built.

`undoLastAccepted()` is the safety net, bound to `U` so a Bluetooth page-turner pedal can
drive it with a foot.

### Showing the fit, not just computing it

`MelodyFitView` on the suggestion card puts the melody's stress pattern and the line's on
one axis, syllable by syllable, with a caption counting the agreement. It exists because
the first question a songwriter asks is *"is this fitted to my melody, or guessing?"* and
the app had no answer: both halves of the proof were on screen in different places, which
asks the reader to hold two patterns in their head. Nobody does that, so the most
distinctive thing the app does read as though it might be theatre.

This is also why `SyllableCounter`'s accuracy is load-bearing beyond ranking: the fit is
drawn position by position, so an over-counted syllable does not merely mis-score a line,
it shifts every comparison after it.

Earlier this behaviour committed the line only when the *next* phrase arrived. A writer
who played one phrase and then stopped to look had kept nothing — the feature existed and
was invisible. Do not reintroduce that ordering.

---

## 6. Testing and verification

Swift Testing (`import Testing`, `@Test`, `#expect`), not XCTest.

```
sudo xcode-select --switch /path/to/Xcode.app/Contents/Developer   # see §8
swift test --package-path Packages/Domain
swift test --package-path Packages/MelodyKit
swift test --package-path Packages/LyricEngine
swift test --package-path Packages/PersistenceKit
```

`SongFinisherTests` needs the Xcode project (`xcodegen generate`), not SwiftPM.

### The lexicon is ground truth — use it

`Resources/lexicon.bin` carries CMUdict-derived syllable counts, per-syllable stress, and
per-syllable vowel openness for 35,346 words. Domain's heuristics — which cannot read it,
since Domain may not import LyricEngine — can therefore be *measured* rather than argued
about. Two real defects were found this way and both were shipped as fixes:

| Heuristic | Was | Now |
|---|---|---|
| `SyllableCounter` syllable count | 82.5% exact | **87.8%** — silent "-ed" unless after t/d, plus a shared syllabic-l test |
| `SingabilityScorer` open vowel | 79.8% | **86.0%** — AY/EY/AW/OW/ER spellings, magic-e, final -y |

(Percentages are over the 34,563 alphabetic entries in `lexicon.bin`. An earlier revision of
this table quoted 83.1% → 88.1% against a smaller high-frequency subset; same change, same
gain, different denominator.)

The second mattered because the old rule called **sky, high, time, eye, my, try, fly,
light, night** and **fire** closed — the words a singer actually holds — so the ranker was
demoting the assembler's best picks for long notes.

The syllable rule went through two rounds. The first handled the syllabic l only on the
"-le" side ("little", "gentle"), which then read every "-le" verb in the past tense —
"handled", "settled", "troubled", "assembled" — a syllable short. `hasSyllabicL` now answers
the question once for both endings and excludes a doubled l, which is what keeps "called"
and "filled" at one syllable while "belle" and "Nashville" stay right.

Before changing either heuristic, port it to Python, run it over the lexicon, and keep the
change only if the number improves. Two attempts failed that test and were dropped:

- A first "-le" rule fired on any consonant plus "e", turned "shine" and "grace" into two
  syllables, and scored *worse* than the original.
- An "-sle" case, added to fix "aisle" and "isle", fixed exactly three words and broke
  exactly three others ("hassle", "tussle", "lisle"). Net zero, so it is not in the
  heuristic; those two words live in `SyllableDictionary` instead, along with the adjectival
  "-ed" class ("naked", "wicked", "sacred", "crooked") that carries no spelling signal at
  all — "naked" and "baked" are identical to any rule.

### Verifying without a Swift toolchain

Agents often run in Linux containers where Swift is unreachable and CI is broken (§7).
These tools need no toolchain and read the *real* lexicon:

- `tools/offline_engine_replica.py` — the assembler pipeline
- `tools/verify_sparks.py` — `imageWords`, with the word sets **parsed out of the Swift
  sources** so it always checks what ships
- `tools/verify_syllables.py` — `SyllableCounter`'s heuristic against CMUdict counts.
  `--compare` prints every rule variant side by side; `--words shine grace handled` prints
  per-word counts. This is the harness the table above came from.
- `tools/check_no_network.sh` — the no-networking guarantee, enforced against the source

These are not decoration. `verify_sparks.py` caught the frozen-spark-panel defect that
review by eye had missed, and `verify_syllables.py` is the only reason two plausible-looking
rule changes did not ship as regressions.

### The latency test

`generationStaysWellUnderTheLatencyBudget` measures **thread CPU time**, best of three,
with a deliberately loose ceiling. History worth knowing:

- Wall-clock, single sample: identical binaries measured 156 ms and 827 ms on consecutive
  runs. `.serialized` only orders cases *within* a suite; sibling suites run in parallel
  and were eating the cores.
- CPU time fixed most of it but **not all** — a thread stalled on a cache miss is still
  on-core accumulating CPU time, and `LexiconSparkProviderTests` streams the whole lexicon
  per case in parallel with it.

So the ceiling is a tripwire for something *categorically* slower (an accidental O(n) over
the lexicon in the beam loop), not a performance spec. **The <100 ms-on-A15 product target
is a release build on device and no host test substitutes for it — it is unvalidated.**

---

## 7. CI is red and it is not the code

`linux-logic-tests` fails on every branch including `main`, and has since the workflow was
created. Every run: **`runner_id: 0`, empty `runner_name`, ~2 seconds, no steps recorded,
404 on the log download.** That is a job no runner ever picked up — not a job whose tests
failed. A real failure has a runner, minutes of output, and a downloadable log.

Already tested and ruled out: job-level `container: swift:6.0` versus pulling the same
image from an ordinary `docker run` step — identical failure, so the workflow is not the
cause. Do not re-run that experiment; the finding is recorded in the workflow file.

What fits: hosted runners unavailable to the account. **Settings → Actions → General**
plus the billing page — a lapsed payment disables Actions account-wide even on a public
repo where minutes are free. Only Mark can fix it.

Consequence: **nothing is verified by CI.** Every change needs a local run.

---

## 8. Environment traps

- **Xcode lives on an external drive** (`/Volumes/Sandisk/Applications/Xcode.app`). If it
  is unplugged, `xcode-select` falls back to Command Line Tools and you get a wall of
  `no such module 'Testing'` and `SwiftDataMacros ... plugin not found` errors that look
  like source bugs but are not. Should be moved to the internal drive before any
  TestFlight archive.
- **`swift test` from `~` fails** with `chdir error` — `cd` into the repo first.
- In agent containers, `download.swift.org` is blocked at the proxy (403). There is no way
  to get a toolchain; use the Python tools in §6.
- **Signing lives in `Signing.xcconfig`, not in Xcode's UI.** `xcodegen generate` rewrites
  the project file and discards whatever the Signing & Capabilities tab was set to, so a
  fix made there survives until the next regeneration and then silently reverts. Both the
  app and `SongFinisherWidgets` read the xcconfig; an extension that cannot sign fails the
  whole install even when the app builds cleanly.
- **On an unenrolled Apple ID the development profile expires after seven days.** The
  installed app then stops launching and iOS shows *"app is no longer available"* over a
  greyed-out icon. That is not a build failure. Delete the dead icon (a stub with an
  invalid profile blocks the reinstall) and rebuild from Xcode.

---

## 9. Branch and PR conventions

Work on `claude/generic-lyrics-ew8chh` for this repo, push there, open **draft** PRs.
A merged PR is finished — restart the branch from `main` rather than stacking onto it.

Open work is PR #7.

---

## 10. What is not done

- **The iOS target has never been built.** macOS has; iOS has not, not once. Everything
  under `#if os(iOS)` — the Live Activity, App Intents, the widget extension, the Open
  Settings buttons — is unproven. This is the single largest unknown in the project.
- **A15 latency unvalidated** (§6).
- **Vowel *identity* is not detected.** The app knows how long a note is and whether a word
  is open, so it puts singable vowels on held notes. It does **not** hear *which* vowel was
  sung — there is no formant analysis anywhere in MelodyKit. Matching the actual sung vowel
  would need F1/F2 tracking, a vowel classifier, and a new field on `PhraseSpec`.
- **Bio inconsistency**: the book says Sync Musica, the launch pack says Rexius Records.
  Apple verifies this — pick one before submitting.
- **The repo is public**, which exposes `docs/APP_STORE*.md` and this file.
- **Apple Developer enrollment** ($99) not confirmed complete.
- GitHub Pages needs pointing at the `gh-pages` branch (root) — `privacy.html` and
  `support.html` are written and must be reachable at a public URL for App Review.
- `support.html` still describes flow mode as "playing your next phrase keeps the line".
  That is now wrong — every line is kept as it arrives. Note that the marketing site is
  **not in this repository**: it lives on the `gh-pages` branch, so it does not move with
  a change to `main` and has to be updated separately, before launch.

---

## 11. Working with Mark

He asks for the cold hard truth and means it. He is a solo developer shipping his first
app and he can tell when he is being managed. Tell him what is broken, what you did not
verify, and what you got wrong — he has caught real errors that way, including two in this
session. Do not report something as working when it has only been read, not run.
